{
  "name": "bondova-mor",
  "version": "0.1.0",
  "description": "bondova-MOR: A system where Skape generates ideas that are analyzed by our custom-built analysis motor.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src tests",
    "lint:fix": "eslint src tests --fix"
  },
  "keywords": [
    "skape",
    "motor",
    "analysis",
    "ideas"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
