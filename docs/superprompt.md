# bondova-MOR Project Guide

**Last Updated:** 2026-09-23

## Vision

bondova-MOR is a system designed to:
1. Receive ideas generated by **Skape** (external ideas source)
2. Analyze those ideas using our custom-built **Analysis Motor**
3. Store and manage analysis results for further use

## Project Name Meaning

- **bondova** - Project identifier
- **MOR** - Motor Operation and Research (our custom analysis system)

## Architecture Overview

```
Skape (Ideas Source)
    ↓
    ├─→ [Skape Adapter] - Normalize incoming ideas
    │
    ↓
[Analysis Motor] - Core analysis engine
    │
    ├─→ Processing Pipeline
    ├─→ Idea Evaluation
    ├─→ Result Generation
    │
    ↓
[Storage Layer] - Persist results
    │
    ├─→ Database (TBD: Supabase/PostgreSQL/etc)
    ├─→ State Management
    ├─→ Query Interface
    │
    ↓
Output (Analysis Results)
```

## Core Components

### 1. Skape Adapter (`src/skape-adapter/`)
- Interface for receiving ideas from Skape
- Data validation and normalization
- Error handling for malformed input
- Transforms external format → internal format

### 2. Analysis Motor (`src/motor/`)
- **Implementation:** Powered by **Grok API** via Anthropic SDK
- Core analysis logic with Grok's reasoning capabilities
- Single `analyze()` method for idea evaluation
- Batch processing support via `analyzeBatch()`
- Structured analysis output with feasibility, impact, novelty assessment
- Performance optimized for single and batch processing

### 3. Storage Layer (`src/storage/`)
- Database abstraction layer
- State persistence
- Query interface for retrieving analysis results
- Transaction support where needed

## Data Flow

1. Skape generates an idea and submits via Adapter
2. Adapter validates and normalizes the idea
3. Motor receives normalized idea
4. Motor executes analysis pipeline
5. Results stored in database
6. Results available for querying and further processing

## Technology Stack

- **Runtime:** Node.js
- **Package Manager:** npm
- **Analysis Engine:** Grok API (via Anthropic SDK)
- **Database:** (To be determined based on requirements)
- **Testing:** Jest or similar
- **CI/CD:** GitHub Actions

## Development Phases

### Phase 1: Foundation & Structure ✓ (Complete)
- [x] Define Architecture
- [x] Add Starter Documentation
- [x] Initialize Project Structure

### Phase 2: Core Implementation
- [ ] Skape Integration (Adapter)
- [x] Analysis Motor Implementation (Grok-powered) ✓ Phase 2a: Complete
- [ ] Storage & State Management

### Phase 3: Operations & CI/CD
- [ ] GitHub Actions Workflows
- [ ] Deployment Configuration
- [ ] Local Development Documentation

## Key Decisions

- **Version Control:** Git with conventional commits
- **Code Style:** ESLint configured project standards
- **Testing:** Test-driven development encouraged
- **Documentation:** Keep docs/ updated with architectural decisions

## Environment Variables

Configuration via environment variables (see `.env.example`):

- `NODE_ENV` - Environment (development/production/test)
- `DATABASE_URL` - Database connection string
- `SKAPE_API_KEY` - API key for Skape integration (if applicable)
- `SKAPE_API_URL` - API URL for Skape integration
- `GROK_API_KEY` - **Required** - API key for Grok analysis engine
- `MOTOR_BATCH_SIZE` - Batch size for idea processing (default: 100)
- `MOTOR_TIMEOUT_MS` - Motor processing timeout in milliseconds (default: 30000)

## Using the Grok Analysis Motor

### Basic Usage

```javascript
import GrokMotor from './src/motor/grok-engine.js';

// Initialize motor with API key
const motor = new GrokMotor(process.env.GROK_API_KEY);

// Analyze a single idea
const idea = {
  id: 'idea-1',
  title: 'Sustainable Energy Solution',
  description: 'Develop a new solar panel technology with 50% higher efficiency',
  tags: ['energy', 'sustainability', 'technology']
};

const analysis = await motor.analyze(idea);
console.log(analysis);

// Output includes:
// - feasibility assessment
// - impact evaluation
// - novelty assessment
// - key strengths and challenges
// - next steps recommendation
// - overall summary
```

### Batch Processing

```javascript
const ideas = [
  { id: 'idea-1', title: '...', description: '...', tags: [...] },
  { id: 'idea-2', title: '...', description: '...', tags: [...] },
  // ... more ideas
];

const analyses = await motor.analyzeBatch(ideas);
// Returns array of analysis results
```

### Running with Demo

```bash
npm start -- --demo
```

This analyzes an example idea and displays the Grok analysis output.

## Getting Started

1. Clone the repository
2. Install dependencies: `npm install`
3. Copy `.env.example` to `.env.local` and fill in values:
   - **Required:** `GROK_API_KEY` - Get from your Anthropic/Grok account
   - Optional: `DATABASE_URL`, `SKAPE_API_KEY`, etc.
4. Run tests: `npm test`
5. Start development: `npm start` or `npm run dev`
6. Try the demo: `npm start -- --demo`

## Testing Strategy

- Unit tests for individual components
- Integration tests for data flow between components
- End-to-end tests for complete idea processing
- Test files in `tests/` directory mirroring `src/` structure

## Future Considerations

- Scalability: Can motor handle high-volume idea processing?
- Extensibility: How do we add new analysis types?
- Monitoring: How do we track motor performance and errors?
- Versioning: How do we manage changes to analysis algorithms?

## Useful Resources

- [CONTRIBUTING.md](../CONTRIBUTING.md) - How to contribute
- [README.md](../README.md) - Project overview
- [AGENTS.md](../AGENTS.md) - AI agent guidance
